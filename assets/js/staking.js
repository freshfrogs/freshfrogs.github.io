// assets/js/staking.js
// Placeholder for staking-specific wiring (kept minimal so it never blocks UI)
// You can expand this later to call your contract methods using CONTROLLER_ABI.

export function noop() {}
